export interface ReportIndex {
    id: number,
    name: string,
    sortIndex: number
}

export interface ReportInfo {
    id: number;
    report: number;
    reportName: string;
    label: string;
    text: string;
}